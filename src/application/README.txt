// ============================================================
// application 层：应用服务（需求文档要求）
// 当前只保留 ModelDocument：UI 和 AI 共用的模型入口与唯一所有者。
// Undo/Redo 是它的私有实现；未来模型变大时可替换历史策略而不改变调用方。
// 作用：把 domain(业务概念)和 ui(界面)串起来的中层
// ============================================================
